# Copy this file to .env (inside the frontend folder).
# Leave VITE_BACKEND_URL empty if you deploy with the included
# vercel.json — it proxies /api/* and /auth/* to your backend,
# so the app talks to its own origin and no value is needed.
VITE_BACKEND_URL=
